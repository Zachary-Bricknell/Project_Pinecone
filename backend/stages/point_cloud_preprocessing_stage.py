# Preprocessing goes here, default text to update in git

def preprocessing_stage(filepath, current_step, stage_prefix):
    """
    Def function here. Must return the filepath and a boolean flag if all steps are completed. 
    """
    return filepath, True